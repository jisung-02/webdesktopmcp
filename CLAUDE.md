# Claude Code instructions

Load the shared repository instructions:

@AGENTS.md

Keep project rules in `AGENTS.md` rather than duplicating them here. Use the commands and validation scope defined there. Keep machine-specific memory, credentials, permission overrides, and personal skills outside this shared file.
